{% extends 'base.html.twig' %}

{% block title %}Etiquettes{% endblock %}

{% block body %}
    <h1>Etiquettes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etiquette.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ etiquette.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etiquettes_index') }}">back to list</a>

    <a href="{{ path('etiquettes_edit', {'id': etiquette.id}) }}">edit</a>

    {{ include('etiquettes/_delete_form.html.twig') }}
{% endblock %}
