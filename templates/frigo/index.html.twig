{% extends 'base.html.twig' %}

{% block title %}Frigo index{% endblock %}

{% block body %}
    <h1>Frigo index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for frigo in frigos %}
            <tr>
                <td>{{ frigo.id }}</td>
                <td>{{ frigo.nom }}</td>
                <td>
                    <a href="{{ path('frigo_show', {'id': frigo.id}) }}">show</a>
                    <a href="{{ path('frigo_edit', {'id': frigo.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('frigo_new') }}">Create new</a>
{% endblock %}
